🤖 Task 03: Decision Tree Classifier
Goal: Build a model to predict if a client will subscribe to a bank term deposit.

Dataset: Bank Marketing (UCI Repository).

Methodology: * Preprocessing: One-Hot Encoding for categorical variables.

Modeling: Trained a DecisionTreeClassifier with a max_depth of 3 to avoid overfitting.

Key Insight: The "duration" of the last contact was the strongest predictor of success.
